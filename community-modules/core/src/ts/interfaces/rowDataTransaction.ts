/** Contains Row Data, the data that you are providing to the grid. */
export interface RowDataTransaction {
  /** deprecated */ addIndex?: number;
  add?: any[];
  remove?: any[];
  update?: any[];
}
